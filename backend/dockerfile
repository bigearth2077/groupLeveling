# 1. 构建阶段
FROM golang:1.24-alpine AS builder

# 设置 Go 代理，加速国内下载
ENV GOPROXY=https://goproxy.cn,direct

# 安装必要的系统工具（虽然 Go 通常不需要，但有些 CGO 依赖可能需要）
RUN apk add --no-cache git

WORKDIR /app

# 预先复制 go.mod 和 go.sum 缓存依赖
COPY go.mod go.sum ./
RUN go mod download

# 复制源代码
COPY . .

# 编译应用
# CGO_ENABLED=0 表示禁用 CGO，构建纯静态二进制文件，兼容性更好
# -o server 指定输出文件名
RUN CGO_ENABLED=0 GOOS=linux go build -o server ./cmd/server/main.go

# 2. 运行阶段
FROM alpine:latest

# 安装基础库和时区数据（非常重要，因为你的业务逻辑依赖 Asia/Shanghai）
RUN apk --no-cache add ca-certificates tzdata

# 设置时区
ENV TZ=Asia/Shanghai

WORKDIR /root/

# 从构建阶段复制二进制文件
COPY --from=builder /app/server .

# 暴露端口
EXPOSE 8080

# 启动命令
CMD ["./server"]