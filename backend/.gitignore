# --- 1. 编译产物 (Binaries) ---
# 忽略编译出来的可执行文件
server
main
*.exe
*.exe~
*.dll
*.so
*.dylib

# --- 2. 依赖与临时文件 ---
# Go 的 vendor 目录 (如果你不使用 vendor 模式，可以忽略这个文件夹)
vendor/

# --- 3. 环境变量与密钥 (Secrets) ---
# 极其重要！防止密码泄露
.env
.env.local
config/config.yaml
# 如果你想保留一个配置模板，可以创建一个 config/config.example.yaml 提交上去
# 但真正的 config.yaml 要忽略

# --- 4. Docker 挂载的数据目录 (Data Volumes) ---
# 对应 docker-compose.yml 里的 volumes
pg_data/
redis_data/

# --- 5. IDE 配置 (IDE Settings) ---
# VS Code
.vscode/
# GoLand / IntelliJ
.idea/
*.iml

# --- 6. 系统文件 (OS Files) ---
# Mac
.DS_Store
# Windows
Thumbs.db